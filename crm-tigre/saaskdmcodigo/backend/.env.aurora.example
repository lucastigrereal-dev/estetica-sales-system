# ===================================
# AURORA (IA) - OpenAI Configuration
# ===================================

# OpenAI API Key (obrigatório)
# Obtenha em: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-api-key-here

# Modelo OpenAI a ser usado (opcional, padrão: gpt-4-turbo-preview)
# Opções: gpt-4-turbo-preview, gpt-4, gpt-4o-mini
OPENAI_MODEL=gpt-4-turbo-preview

# Threshold de qualificação (opcional, padrão: 70)
# Leads com score >= 70 são considerados qualificados
AURORA_THRESHOLD_QUALIFICADO=70

# ===================================
# INSTRUÇÕES DE USO
# ===================================
#
# 1. Copie este arquivo para .env no mesmo diretório
# 2. Substitua OPENAI_API_KEY pela sua chave real
# 3. Reinicie o servidor backend
#
# 4. Configure os procedimentos disponíveis no banco:
#    INSERT INTO Settings (key, value, companyId, createdAt, updatedAt) VALUES
#    ('procedimentosDisponiveis', 'Botox,Preenchimento,Limpeza de Pele,Peeling,Harmonização Facial', 1, NOW(), NOW()),
#    ('horarioFuncionamento', 'Segunda a Sexta: 9h às 18h, Sábado: 9h às 13h', 1, NOW(), NOW());
#
# 5. Rode as migrations:
#    npm run db:migrate
#
# ===================================
# CUSTOS ESTIMADOS (OpenAI API)
# ===================================
#
# GPT-4 Turbo:
# - Input: $0.01 / 1K tokens
# - Output: $0.03 / 1K tokens
# - Custo médio por conversa (10 mensagens): ~$0.01
# - 1000 conversas/mês: ~$10
#
# GPT-4o-mini (mais econômico):
# - 50% mais barato
# - Ideal para sentimento e resumos
#
